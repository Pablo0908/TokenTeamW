Style the following Vue component using Tailwind CSS + DaisyUI.

$ARGUMENTS

Rules:
- DO NOT change logic, props, emits, or reactive data — classes only
- Hierarchy: DaisyUI first → Tailwind for fine-tuning → custom CSS only as last resort
- Dark theme: `bg-base-100`, `bg-base-200`, `bg-base-300`, `text-base-content`
- Buttons: `btn btn-primary`, `btn btn-secondary`, `btn btn-error btn-sm`
- Cards: `card bg-base-100 shadow-xl`
- Inputs: `input input-bordered bg-neutral w-full`
- Active state: `badge badge-success`
- Upcoming state: `badge badge-secondary`
- Past state: `badge badge-ghost opacity-50`
- Mobile-first: must look good on mobile first
- Do not use fixed colors if DaisyUI already covers them

Return only the complete component with styles applied.