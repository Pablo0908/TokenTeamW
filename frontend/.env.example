# --- API ---
# Base URL of the Flask API. Local dev points at the backend on 127.0.0.1:5000.
# Use 127.0.0.1 (not "localhost"): on many Windows setups localhost resolves to
# IPv6 (::1) first while the dev server listens on IPv4, adding ~200ms per request.
# In production (Vercel) set this to the Render API origin, e.g.
#   VITE_API_URL=https://tokenteamw-api.onrender.com
VITE_API_URL=http://127.0.0.1:5000

# --- Demo / mock mode ---
# When "true", the app serves built-in sample data (no backend needed) so you can
# preview every screen exactly as designed. Set to "false" once the Flask API is
# running and VITE_API_URL points at it.
VITE_USE_MOCK=true

# Base URL the QR codes encode (the deployed frontend origin). Used by the admin
# panel when displaying the redeem link for a generated QR.
# Use your LAN IP (e.g. http://192.168.x.x:5173) if QR codes need to open on phones.
VITE_PUBLIC_URL=http://127.0.0.1:5173

# --- Google OAuth ---
# Google Sign-In client ID. Must match GOOGLE_CLIENT_ID in backend/.env.
# IMPORTANT: before the "Sign in with Google" button works, you must add these
# as authorized JavaScript origins in Google Cloud Console:
#   Google Cloud Console > APIs & Services > Credentials > OAuth 2.0 client ID
#     http://localhost:5173
#     http://127.0.0.1:5173
# Without that step the button returns Error 400: origin_mismatch.
VITE_GOOGLE_CLIENT_ID=14203702857-s0r7c79qplmph9f2t0b693qage7m6fr5.apps.googleusercontent.com
